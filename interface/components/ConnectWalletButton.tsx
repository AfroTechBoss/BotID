export default function ConnectWalletButton() {
  // Stub — wire wagmi/viem/RainbowKit here (§11, §20.7). Read-only pages must render fully
  // without a connected wallet; only /portal gates on connection.
  return <button className="btn btn-secondary">Connect wallet</button>;
}
